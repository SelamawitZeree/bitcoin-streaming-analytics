package cs590.project.customer.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class XRayConfig {
    // X-Ray configuration - temporarily disabled due to Jakarta servlet compatibility
    // Will be enabled after updating to Jakarta-compatible X-Ray SDK version
}

